# Local development environment - uses Vite proxy to local backend
# This ensures local development uses your local test data
# Leave empty to use default proxy configuration
VITE_API_BASE_URL=

# To test against production backend, uncomment the line below:
# VITE_API_BASE_URL=https://mangakg-backend.fly.dev/api